# TrainAndTest
This is an assignment of "getting and cleaning data".

The R script "run_analysis.R" is the complete code which tides the raw data into a clean data set.

The code book "CodeBook.md" explains the steps performed to clean up the data and describes the variables in the tidy data set.

